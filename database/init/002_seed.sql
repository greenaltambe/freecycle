-- =====================================================================
-- Optional dev seed data.
-- Categories are seeded in 001_schema.sql.
-- Real users should register through /api/auth/register so passwords are
-- hashed by bcrypt at runtime. Run this file manually if you need fixture
-- data for tests.
-- =====================================================================

SELECT 'Schema initialized.' AS status;
