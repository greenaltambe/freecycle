{
  "name": "freecycle-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Local Free Stuff Platform - microservices monorepo (Node.js + React + PostgreSQL/PostGIS).",
  "workspaces": [
    "services/*",
    "shared",
    "frontend"
  ],
  "scripts": {
    "up": "docker compose down --remove-orphans && docker compose up --build",
    "down": "docker compose down --remove-orphans -v",
    "restart": "docker compose down --remove-orphans && docker compose up --build -d",
    "logs": "docker compose logs -f",
    "test": "npm run test --workspaces --if-present"
  }
}
